// Core stub (R1f) — Starter finish-swatch renderer excluded from the public
// Core tree. Renders null (no swatch UI in Core). Same props as the Starter
// component it replaces.
export default function FinishSwatch(_props: {
  name: string;
  size?: number;
  withTooltip?: boolean;
}): null {
  return null;
}
