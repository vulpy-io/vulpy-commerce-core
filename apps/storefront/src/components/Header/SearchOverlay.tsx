"use client";

/** Core no-op for the Starter search overlay. Core catalog/faceted search remains available. */
export default function SearchOverlay(_props: Record<string, unknown>) {
  return null;
}
